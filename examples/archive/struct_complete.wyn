struct Point {
    x: int
    y: int
}

fn main() -> int {
    var p = Point { x: 10, y: 20 }

        print(p.x)
        print(p.y)

        var sum = p.x + p.y
        print(sum)

        return 0
    }
