// Simplified Arc test - basic functionality only
fn main() -> int {
    // Test basic reference counting concepts
    var x = 42;
    var y = x;  // Copy value
    
    // Test that values are independent
    x = 100;
    
    // y should still be 42
    if y != 42 {
        return 1;
    }
    
    return 0;
}
