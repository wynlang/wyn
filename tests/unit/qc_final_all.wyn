// Final QC: All working features
struct Point { x: int, y: int }
fn add(a: int, b: int) -> int { return a + b; }
fn identity<T>(x: T) -> T { return x; }

trait Display {
    fn show() -> int;
}

fn main() -> int {
    let sum = add(10, 5);
    let p = Point { x: 3, y: 4 };
    let val = identity(42);
    let arr = [10, 20, 30];
    
    let result = match sum {
        15 => arr[1],
        _ => 0
    };
    
    match p.x {
        3 => return 20,
        _ => return 0
    }
}
