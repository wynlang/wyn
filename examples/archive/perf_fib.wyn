fn fib_iterative(n: int) -> int {
    if n <= 1 {
        return n
    }
    var a = 0
    var b = 1
    for var i = 2; i <= n; i + = 1 {
        var temp = a + b
        a = b
        b = temp
    }
    return b
}

fn main() -> int {
    var start = time_now()

    for var i = 0; i < 1000; i + = 1 {
        var result = fib_iterative(20)
    }

    var end = time_now()
    print(end - start)

    return 0
}
