fn make_adder(n: int) -> fn(int) -> int {
    return fn(x: int) => x + n;  // Captures 'n'
}

fn main() -> int {
    var add5 = make_adder(5);
    var result = add5(10);  // Should return 15
    return 0;
}