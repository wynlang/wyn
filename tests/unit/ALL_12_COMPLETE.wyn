struct Point { x: int, y: int }
enum Status { PENDING, DONE }
type UserId = int;

fn add(a: int, b: int) -> int { return a + b; }
fn identity<T>(val: T) -> T { return val; }
fn Point.distance(a: int, b: int) -> int { return a + b; }
fn worker() -> int { return 1; }

fn main() -> int {
    var y = 20; y = 30;
    var sum = add(10, 20);
    var p = Point { x: 3, y: 4 };
    var s = DONE;
    var arr = [1, 2, 3];
    var success = ok(42);
    var maybe = some(42);
    var result = match p.x { 3 => 88, _ => 0 };
    if result == 88 { var temp = 1; }
    while y < 35 { y = y + 1; }
    for i in 0..5 { if i == 2 { break; } }
    var id: UserId = 123;
    var val = identity(42);
    var ext_result = Point_distance(3, 4);
    spawn worker();
    return result;
}
