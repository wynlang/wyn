// Realistic program using all working features

struct Config {
    width: int,
    height: int
}

fn calculate<T>(value: T) -> T {
    return value;
}

fn get_area(w: int, h: int) -> int {
    return w * h;
}

trait Renderable {
    fn render() -> int;
}

fn main() -> int {
    var config = Config { width: 10, height: 20 };
    var area = get_area(config.width, config.height);
    
    var values = [100, 200, area];
    var selected = values[2];
    
    var result = match selected {
        200 => 1,
        _ => 2
    };
    
    var final_value = calculate(result);
    
    match final_value {
        2 => return 42,
        _ => return 0
    }
}
