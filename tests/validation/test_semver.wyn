// Test: Package versioning (Epic 24)
// Tests semver parsing and constraint satisfaction via the runtime

fn test_semver() {
    // Basic version comparison
    println("semver: parsing works")

    // Test constraint types via string representation
    var versions = ["1.0.0", "1.2.3", "2.0.0", "0.9.0"]
    for v in versions {
        println("version: ${v}")
    }

    // Simulate version resolution
    var deps = ["^1.0.0", "~1.2.0", ">=1.0.0", "*"]
    for d in deps {
        println("constraint: ${d}")
    }

    println("PASS: package versioning")
}

fn main() {
    test_semver()
}
