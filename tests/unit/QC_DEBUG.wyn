fn main() -> int {
    var area = 10 * 20;
    var values = [100, 200, area];
    var selected = values[2];
    
    var result = match selected {
        200 => 1,
        _ => 2
    };
    
    return result;
}
