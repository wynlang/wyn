type UserId = int;
type Score = int;
type Distance = int;

fn main() -> int {
    var user: UserId = 100;
    var score: Score = 95;
    var dist: Distance = 50;
    
    return user + score + dist;  // 245
}
