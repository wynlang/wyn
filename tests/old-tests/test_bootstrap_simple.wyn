// Simple test for bootstrap - can the Wyn compiler compile itself?
// Start with the simplest possible Wyn program

fn add(a: int, b: int) -> int {
    return a + b
}

fn main() -> int {
    var x = add(10, 20)
    return x
}
