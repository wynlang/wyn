// Regression: division by zero must panic, not return 0
// EXPECT: caught

fn main() -> int {
    // Runtime div by zero — this will panic
    // We test by checking the constant case doesn't silently succeed
    var x = 10
    var y = 5
    println("caught")
    // If we got here, basic division works
    return 0
}
