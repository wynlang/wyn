// TDD: Test enum with match
enum Color { RED, GREEN, BLUE }

fn main() -> int {
    var c = RED
    match c {
        RED => { return 1; }
        GREEN => { return 2; }
        BLUE => { return 3; }
        _ => { return 0; }
    }
}
