fn main() -> int {
    pool_add_task("date +%s.%N && sleep 1 && date +%s.%N");
    pool_add_task("date +%s.%N && sleep 1 && date +%s.%N");
    pool_start(2);
    var failures = pool_wait();
    return failures;
}
