// Crypto module functions
fn hash(val: int) -> int {
    var h = 5381;
    var v = val;
    var i = 0;
    while i < 4 {
        h = ((h * 33) + (v % 256));
        v = v / 256;
        i = i + 1;
    }
    return h;
}

fn checksum(data: int) -> int {
    var cs = 0;
    var d = data;
    while d > 0 {
        cs = cs + (d % 256);
        d = d / 256;
    }
    return cs % 256;
}

fn main() -> int {
    var h = hash(12345);
    var c = checksum(1000);
    return c;  // Should return checksum of 1000
}
