struct Vec2 { x: int, y: int }
struct Vec3 { x: int, y: int, z: int }

fn add_vec2(a: Vec2, b: Vec2) -> Vec2 {
    return Vec2 { x: a.x + b.x, y: a.y + b.y };
}

fn magnitude<T>(v: T) -> int {
    return 1;
}

trait Drawable {
    fn draw() -> int;
}

fn process_array(arr: [int]) -> int {
    return arr[0];
}

fn main() -> int {
    let v1 = Vec2 { x: 10, y: 20 };
    let v2 = Vec2 { x: 5, y: 15 };
    let v3 = add_vec2(v1, v2);
    
    let arr = [v3.x, v3.y, 100];
    
    let result = match v3.x {
        15 => arr[2],
        _ => 0
    };
    
    let mag = magnitude(v3);
    
    match result {
        100 => return 50,
        _ => return 0
    }
}
