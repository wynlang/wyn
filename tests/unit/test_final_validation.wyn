// Final validation: All working features
struct Point { x: int, y: int }
fn add(a: int, b: int) -> int { return a + b; }
fn identity<T>(x: T) -> T { return x; }

fn main() -> int {
    let sum = add(10, 5);
    let p = Point { x: 3, y: 4 };
    let val = identity(42);
    let arr = [100, 200, 300];
    
    let result = match sum {
        15 => arr[2],
        _ => 0
    };
    
    match p.x {
        3 => return 77,
        _ => return 0
    }
}
