enum Result {
    Ok(int),
    Err(string)
}

fn main() -> int {
    var x = Result::Ok(42);
    
    match x {
        Result::Ok(n) => {
            print(n);
            return n;
        },
        Result::Err(e) => {
            print("Error");
            return 0;
        }
    }
}