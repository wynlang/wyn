fn compute(n: int) -> int {
    return n * n;
}

fn main() -> int {
    var f1 = spawn compute(5);
    var r1 = f1.await();
    
    var f2 = spawn compute(r1);
    var r2 = f2.await();
    
    if r2 == 625 {
        print(1);
    }
    return 0;
}
