enum Color {
    Red,
    Blue
}

fn check_color(c: Color, expected: Color) -> int {
    if c == expected {
        return 1
    }
    return 0
}

fn main() -> int {
    var c = Color_Red
    var result = check_color(c, Color_Red)
    print(result)
    return 0
}
