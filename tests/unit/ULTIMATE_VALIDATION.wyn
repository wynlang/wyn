struct Data { value: int }
fn process<T>(x: T) -> T { return x; }
trait Handler { fn handle() -> int; }

fn main() -> int {
    var d = Data { value: 15 };
    var arr = [10, 20, 30];
    var x = some(42);
    
    var result = match d.value {
        15 => arr[1],
        _ => 0
    };
    
    match result {
        20 => return process(20),
        _ => return 0
    }
}
