// T3.2.3: Standard Traits Test
// Testing standard trait usage in Wyn code

// Standard traits are automatically available:
// Eq, Ord, Clone, Copy, Display, Debug, Iterator

// Generic function using standard trait bounds
fn compare_and_display<T>(a: T, b: T) where T: Eq + Display {
    return 0;
}

// Generic function using Clone trait
fn duplicate<T>(item: T) where T: Clone {
    return 0;
}

fn main() {
    // Basic types automatically implement standard traits
    let x = 42;
    let y = 42;
    
    // These should work with standard trait implementations
    compare_and_display(x, y);
    duplicate(x);
    
    let s1 = "hello";
    let s2 = "world";
    
    // String also implements standard traits (except Copy)
    compare_and_display(s1, s2);
    duplicate(s1);
    
    return 0;
}
