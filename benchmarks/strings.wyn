// String operations — method chaining, interpolation
// Tests: string allocation, method dispatch, GC pressure

fn main() -> int {
    var count = 0
    for i in 0..10000 {
        var s = "hello world ${i}"
        if s.contains("5000") { count = count + 1 }
        var u = s.upper()
        var r = u.replace("HELLO", "HI")
        var parts = r.split(" ")
    }
    println("string ops done, matches = ${count}")
    return 0
}
