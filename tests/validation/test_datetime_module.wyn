// TDD: DateTime module - time operations

fn main() -> int {
    var now = DateTime_now()
    // Timestamp should be > 0 (we're past 1970)
    if now > 0 {
        println("timestamp ok")
    }

    var formatted = DateTime_format(now, "%Y-%m-%d")
    println(formatted)

    return 0
}
