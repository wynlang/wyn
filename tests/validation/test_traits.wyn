// TDD: Basic trait system
// Tests trait definition, impl blocks, and trait method calls

trait Display {
    fn to_string(self) -> string
}

struct Point { x: int, y: int }

impl Display for Point {
    fn to_string(self) -> string {
        return "Point(" + self.x.to_string() + ", " + self.y.to_string() + ")"
    }
}

struct Color { r: int, g: int, b: int }

impl Display for Color {
    fn to_string(self) -> string {
        return "Color(" + self.r.to_string() + "," + self.g.to_string() + "," + self.b.to_string() + ")"
    }
}

fn main() -> int {
    var p = Point { x: 10, y: 20 }
    println(p.to_string())

    var c = Color { r: 255, g: 128, b: 0 }
    println(c.to_string())

    return 0
}
