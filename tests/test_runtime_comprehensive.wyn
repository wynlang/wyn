// Comprehensive runtime library test
fn main() -> int {
    // Test System::env
    var path = System::env("PATH")
    // PATH should exist
    
    // Test Time::now
    var t1 = Time::now()
    if t1 <= 0 {
        return 1
    }
    
    // Test Time::sleep (1ms)
    Time::sleep(1)
    
    var t2 = Time::now()
    if t2 <= t1 {
        return 2
    }
    
    // Test File::cwd
    var cwd = File::cwd()
    // CWD should exist
    
    return 0
}
