# Simple Library Package
[package]
name = "math_utils"
version = "2.1.3"
authors = ["Math Team <math@example.com>"]
description = "Mathematical utility functions for Wyn"
license = "Apache-2.0"
repository = "https://github.com/wyn-lang/math_utils"

[dependencies]
# No dependencies - pure math library

[build]
optimization = "size"
target = "native"
debug_symbols = true
