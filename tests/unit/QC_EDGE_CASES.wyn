// Edge case testing

struct Point { x: int, y: int }
struct Empty {}

fn zero() -> int { return 0; }
fn nested_call() -> int { return zero(); }
fn generic_chain<T>(x: T) -> T { return x; }

trait Empty {}
trait Multi {
    fn a() -> int;
    fn b() -> int;
}

fn main() -> int {
    var e = Empty {};
    var p = Point { x: 1, y: 2 };
    var z = nested_call();
    var i = generic_chain(5);
    var arr = [1];
    
    var single = match 1 {
        1 => 10,
        _ => 0
    };
    
    match 99 {
        _ => return 25
    }
}
