// Final verification of all 9 working features
struct Point { x: int, y: int }
fn add(a: int, b: int) -> int { return a + b; }
fn id<T>(x: T) -> T { return x; }
trait T { fn f() -> int; }

fn main() -> int {
    var p = Point { x: 5, y: 10 };
    var s = add(p.x, p.y);
    var v = id(s);
    var a = [10, 20, 30];
    var m = match v { 15 => a[2], _ => 0 };
    var o = some(m);
    match m { 30 => return 30, _ => return 0 }
}
