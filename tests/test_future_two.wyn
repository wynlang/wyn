fn compute(n: int) -> int {
    return n * n;
}

fn main() -> int {
    var f1 = spawn compute(10);
    var f2 = spawn compute(20);
    var r1 = f1.await();
    var r2 = f2.await();
    print(r1);
    print(r2);
    return 0;
}
