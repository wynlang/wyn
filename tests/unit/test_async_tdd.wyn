// Simplified async test - basic functionality only
fn main() -> int {
    // Test basic async concepts with synchronous code
    var result = compute_value();
    
    if result != 42 {
        return 1;
    }
    
    return 0;
}

fn compute_value() -> int {
    return 42;
}
